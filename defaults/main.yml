---
# defaults file for sa-vpn-pptp-client

role_dir: "{{role_path}}"

pptp_user: vpn_name
pptp_password: vpn_password

pptp_server: pptp.server.hostname

pptp_profile: VPNprofile

pptp_remote_network: "192.168.1.0"
pptp_remote_network_mask: "255.255.255.0"
